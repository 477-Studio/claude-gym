name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Build
        env:
          CGO_ENABLED: 1
          GOOS: darwin
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -trimpath -ldflags="-s -w" -o cgym-darwin-${{ matrix.goarch }} .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cgym-darwin-${{ matrix.goarch }}
          path: cgym-darwin-${{ matrix.goarch }}

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev libxi-dev libxcursor-dev libxrandr-dev libxinerama-dev libxxf86vm-dev libwayland-dev libxkbcommon-dev

      - name: Build
        env:
          CGO_ENABLED: 1
          GOOS: linux
          GOARCH: amd64
        run: |
          go build -trimpath -ldflags="-s -w" -o cgym-linux-amd64 .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cgym-linux-amd64
          path: cgym-linux-amd64

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Build
        env:
          CGO_ENABLED: 1
          GOOS: windows
          GOARCH: amd64
        run: |
          go build -trimpath -ldflags="-s -w -H windowsgui" -o cgym-windows-amd64.exe .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cgym-windows-amd64
          path: cgym-windows-amd64.exe

  release:
    needs: [build-macos, build-linux, build-windows]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Prepare release assets
        run: |
          mkdir -p release

          # macOS Intel
          cd dist/cgym-darwin-amd64
          chmod +x cgym-darwin-amd64
          tar -czvf ../../release/claude-gym_${{ github.ref_name }}_darwin_amd64.tar.gz cgym-darwin-amd64
          cd ../..

          # macOS Apple Silicon
          cd dist/cgym-darwin-arm64
          chmod +x cgym-darwin-arm64
          tar -czvf ../../release/claude-gym_${{ github.ref_name }}_darwin_arm64.tar.gz cgym-darwin-arm64
          cd ../..

          # Linux
          cd dist/cgym-linux-amd64
          chmod +x cgym-linux-amd64
          tar -czvf ../../release/claude-gym_${{ github.ref_name }}_linux_amd64.tar.gz cgym-linux-amd64
          cd ../..

          # Windows
          cd dist/cgym-windows-amd64
          zip ../../release/claude-gym_${{ github.ref_name }}_windows_amd64.zip cgym-windows-amd64.exe
          cd ../..

          # Checksums
          cd release
          sha256sum * > checksums.txt
          cd ..

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
